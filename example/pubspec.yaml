name: yust_open_file_x_example
description: Demonstrates how to use the yust_open_file_x plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"
  flutter: ">=3.3.5"

dependencies:
  flutter:
    sdk: flutter
  yust_open_file_x:
    path: ../
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
